{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/*.{css,scss,js,png,gif,ico,jpg,jpeg,svg,json,webp,woff,woff2}"]
  },
  "globalHeaders": {
    "X-Content-Type-Options": "nosniff",
    "Referrer-Policy": "strict-origin-when-cross-origin"
  }
}